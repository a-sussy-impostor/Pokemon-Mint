from starters import Pikachu, Charmander
print("--- Welcome to a Pokémon Battle! ---")
print("You've encountered a wild Charmander!")
# Initialize the Pokémon
player_pkmn = Pikachu()
wild_pkmn = Charmander()
# Pokémon introduction
print("Go, " + player_pkmn.name + "!")
print("Wild " + wild_pkmn.name + " appeared!")
# Loop through battle rounds until a Pokémon faints
while True:
    # Player turn
    print("--- Your turn ---")
    player_move = input("Enter your move: ")
    player_pkmn.attack(wild_pkmn, player_move)
    if wild_pkmn.hp == 0:
        print("You defeated " + wild_pkmn.name + "! Congrats!")
        break
    # Wild Pokémon turn
    print("--- Wild " + wild_pkmn.name + "'s turn ---")
    wild_pkmn.attack(player_pkmn)
    if player_pkmn.hp == 0:
        print("Oh no! Your " + player_pkmn.name + " fainted!")
        break

In starters.py:

class Pokemon:
    def __init__(self, name, type, moves, hp):
        self.name = name
        self.type = type
        self.moves = moves
        self.hp = hp
    def attack(self, enemy, move):
        if move not in self.moves:
            print("That move doesn't exist!")
        else:
            dmg = self.moves[move]
            enemy.hp -= dmg
            print(self.name + " used " + move + "! " + enemy.name + " took " + str(dmg) + " damage.")
            print(enemy.name + "'s HP is now " + str(enemy.hp))
class Pikachu(Pokemon):
    def __init__(self):
        moves = {
            "Thunder Shock": 40,
            "Quick Attack": 30,
            "Iron Tail": 50,
            "Volt Tackle": 80
        }
        super().__init__("Pikachu", "Electric", moves, 100)
class Charmander(Pokemon):
    def __init__(self):
        moves = {
            "Scratch": 30,
            "Ember": 40,
            "Flamethrower": 70,
            "Fire Punch": 75
        }
        super().__init__("Charmander", "Fire", moves, 100)